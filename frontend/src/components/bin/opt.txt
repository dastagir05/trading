<div className="p-6 bg-gray-50 min-h-screen">
      <h1 className="text-2xl font-bold mb-4">{symbol} Option Chain</h1>

      <div className="mb-4">
        <label className="text-sm font-medium">Expiry Date:</label>
        <select
          className="ml-2 px-3 py-1 border border-gray-300 rounded"
          value={selectedExpiry}
          onChange={(e) => setSelectedExpiry(e.target.value)}
        >
          {uniqueExpiries.map((exp, idx) => (
            <option key={idx} value={exp}>
              {exp}
            </option>
          ))}
        </select>
      </div>

      <div className="overflow-x-auto bg-white rounded-lg shadow">
        <table className="w-full text-sm text-left">
          <thead className="bg-gray-100 text-gray-700">
            <tr>
              <th className="p-2">Call OI</th>
              <th className="p-2">Call LTP</th>
              <th className="p-2">Strike</th>
              <th className="p-2">Put LTP</th>
              <th className="p-2">Put OI</th>
            </tr>
          </thead>
          <tbody>
            {filtered.map((item, idx) => (
              <tr key={idx} className="border-t">
                <td className="p-2 text-blue-600">
                  {item.call_options.market_data.oi}
                </td>
                <td className="p-2 text-blue-600">
                  ₹{item.call_options.market_data.ltp}
                </td>
                <td className="p-2 font-semibold">{item.strike_price}</td>
                <td className="p-2 text-red-600">
                  ₹{item.put_options.market_data.ltp}
                </td>
                <td className="p-2 text-red-600">
                  {item.put_options.market_data.oi}
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>