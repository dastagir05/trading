mongodb+srv://<db_username>:<db_password>@users.epxlaen.mongodb.net/?retryWrites=true&w=majority&appName=users

http://localhost:3000/login/oauth2/code/github

app.get("/", (req, res) => {
  res.send("Backend server is running");
});
app.post("/api/login", (req, res) => {
  const { name, email, image, provider } = req.body;
  console.log("New user login:", { name, email, image });

  res.status(200).json({ message: "Login received" });
});